//int rand (void);
//void srand (unsigned int seed);
//int atoi (const char * str);

// 1º está redundante, já que é possível usar void no parametro para indicar que a função é sem retorno
// mas  não é uma boa prática, pois não é claro o que a função faz, sendo melhor optar por deixar o campo 
// de parametros vazio.

// 2º é uma função sem retorno (void), que gera um numero aleatório de tipo unsigned int;

// 3º é uma função que usa passagem por referencia de um ponteiro que aponta para uma string.

